[build]
  # Target only the portfolio project to bypass errors in other workspace folders
  command = "npm run build --workspace=@workspace/portfolio"
  
  # The directory to publish
  publish = "artifacts/portfolio/dist/public"


# Redirects for Single Page Application (SPA) routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
